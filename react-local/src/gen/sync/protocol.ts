// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               v6.32.1
// source: sync/protocol.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "protocol";

export interface PushOp {
  id: string;
  syncHlc: string;
  blob: Uint8Array;
  syncIsDeleted: boolean;
}

export interface DeltaOp {
  id: string;
  syncHlc: string;
  blob: Uint8Array;
  syncIsDeleted: boolean;
  serverVersion: number;
}

export interface Push {
  batchId: string;
  ops: PushOp[];
}

export interface Ready {
}

export interface Delta {
  ackFor: string;
  ops: DeltaOp[];
}

export interface Error {
  code: string;
  message: string;
}

export interface ClientFrame {
  push?: Push | undefined;
}

export interface ServerFrame {
  ready?: Ready | undefined;
  delta?: Delta | undefined;
  error?: Error | undefined;
}

function createBasePushOp(): PushOp {
  return { id: "", syncHlc: "", blob: new Uint8Array(0), syncIsDeleted: false };
}

export const PushOp: MessageFns<PushOp> = {
  encode(message: PushOp, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.syncHlc !== "") {
      writer.uint32(18).string(message.syncHlc);
    }
    if (message.blob.length !== 0) {
      writer.uint32(26).bytes(message.blob);
    }
    if (message.syncIsDeleted !== false) {
      writer.uint32(32).bool(message.syncIsDeleted);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PushOp {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePushOp();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.syncHlc = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.blob = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.syncIsDeleted = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PushOp {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      syncHlc: isSet(object.syncHlc)
        ? globalThis.String(object.syncHlc)
        : isSet(object.sync_hlc)
        ? globalThis.String(object.sync_hlc)
        : "",
      blob: isSet(object.blob) ? bytesFromBase64(object.blob) : new Uint8Array(0),
      syncIsDeleted: isSet(object.syncIsDeleted)
        ? globalThis.Boolean(object.syncIsDeleted)
        : isSet(object.sync_is_deleted)
        ? globalThis.Boolean(object.sync_is_deleted)
        : false,
    };
  },

  toJSON(message: PushOp): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.syncHlc !== "") {
      obj.syncHlc = message.syncHlc;
    }
    if (message.blob.length !== 0) {
      obj.blob = base64FromBytes(message.blob);
    }
    if (message.syncIsDeleted !== false) {
      obj.syncIsDeleted = message.syncIsDeleted;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PushOp>, I>>(base?: I): PushOp {
    return PushOp.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PushOp>, I>>(object: I): PushOp {
    const message = createBasePushOp();
    message.id = object.id ?? "";
    message.syncHlc = object.syncHlc ?? "";
    message.blob = object.blob ?? new Uint8Array(0);
    message.syncIsDeleted = object.syncIsDeleted ?? false;
    return message;
  },
};

function createBaseDeltaOp(): DeltaOp {
  return { id: "", syncHlc: "", blob: new Uint8Array(0), syncIsDeleted: false, serverVersion: 0 };
}

export const DeltaOp: MessageFns<DeltaOp> = {
  encode(message: DeltaOp, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.syncHlc !== "") {
      writer.uint32(18).string(message.syncHlc);
    }
    if (message.blob.length !== 0) {
      writer.uint32(26).bytes(message.blob);
    }
    if (message.syncIsDeleted !== false) {
      writer.uint32(32).bool(message.syncIsDeleted);
    }
    if (message.serverVersion !== 0) {
      writer.uint32(40).int64(message.serverVersion);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeltaOp {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeltaOp();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.syncHlc = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.blob = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.syncIsDeleted = reader.bool();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.serverVersion = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeltaOp {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      syncHlc: isSet(object.syncHlc)
        ? globalThis.String(object.syncHlc)
        : isSet(object.sync_hlc)
        ? globalThis.String(object.sync_hlc)
        : "",
      blob: isSet(object.blob) ? bytesFromBase64(object.blob) : new Uint8Array(0),
      syncIsDeleted: isSet(object.syncIsDeleted)
        ? globalThis.Boolean(object.syncIsDeleted)
        : isSet(object.sync_is_deleted)
        ? globalThis.Boolean(object.sync_is_deleted)
        : false,
      serverVersion: isSet(object.serverVersion)
        ? globalThis.Number(object.serverVersion)
        : isSet(object.server_version)
        ? globalThis.Number(object.server_version)
        : 0,
    };
  },

  toJSON(message: DeltaOp): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.syncHlc !== "") {
      obj.syncHlc = message.syncHlc;
    }
    if (message.blob.length !== 0) {
      obj.blob = base64FromBytes(message.blob);
    }
    if (message.syncIsDeleted !== false) {
      obj.syncIsDeleted = message.syncIsDeleted;
    }
    if (message.serverVersion !== 0) {
      obj.serverVersion = Math.round(message.serverVersion);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeltaOp>, I>>(base?: I): DeltaOp {
    return DeltaOp.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeltaOp>, I>>(object: I): DeltaOp {
    const message = createBaseDeltaOp();
    message.id = object.id ?? "";
    message.syncHlc = object.syncHlc ?? "";
    message.blob = object.blob ?? new Uint8Array(0);
    message.syncIsDeleted = object.syncIsDeleted ?? false;
    message.serverVersion = object.serverVersion ?? 0;
    return message;
  },
};

function createBasePush(): Push {
  return { batchId: "", ops: [] };
}

export const Push: MessageFns<Push> = {
  encode(message: Push, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.batchId !== "") {
      writer.uint32(10).string(message.batchId);
    }
    for (const v of message.ops) {
      PushOp.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Push {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePush();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.batchId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.ops.push(PushOp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Push {
    return {
      batchId: isSet(object.batchId)
        ? globalThis.String(object.batchId)
        : isSet(object.batch_id)
        ? globalThis.String(object.batch_id)
        : "",
      ops: globalThis.Array.isArray(object?.ops) ? object.ops.map((e: any) => PushOp.fromJSON(e)) : [],
    };
  },

  toJSON(message: Push): unknown {
    const obj: any = {};
    if (message.batchId !== "") {
      obj.batchId = message.batchId;
    }
    if (message.ops?.length) {
      obj.ops = message.ops.map((e) => PushOp.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Push>, I>>(base?: I): Push {
    return Push.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Push>, I>>(object: I): Push {
    const message = createBasePush();
    message.batchId = object.batchId ?? "";
    message.ops = object.ops?.map((e) => PushOp.fromPartial(e)) || [];
    return message;
  },
};

function createBaseReady(): Ready {
  return {};
}

export const Ready: MessageFns<Ready> = {
  encode(_: Ready, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Ready {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReady();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): Ready {
    return {};
  },

  toJSON(_: Ready): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<Ready>, I>>(base?: I): Ready {
    return Ready.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Ready>, I>>(_: I): Ready {
    const message = createBaseReady();
    return message;
  },
};

function createBaseDelta(): Delta {
  return { ackFor: "", ops: [] };
}

export const Delta: MessageFns<Delta> = {
  encode(message: Delta, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ackFor !== "") {
      writer.uint32(10).string(message.ackFor);
    }
    for (const v of message.ops) {
      DeltaOp.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Delta {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDelta();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.ackFor = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.ops.push(DeltaOp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Delta {
    return {
      ackFor: isSet(object.ackFor)
        ? globalThis.String(object.ackFor)
        : isSet(object.ack_for)
        ? globalThis.String(object.ack_for)
        : "",
      ops: globalThis.Array.isArray(object?.ops) ? object.ops.map((e: any) => DeltaOp.fromJSON(e)) : [],
    };
  },

  toJSON(message: Delta): unknown {
    const obj: any = {};
    if (message.ackFor !== "") {
      obj.ackFor = message.ackFor;
    }
    if (message.ops?.length) {
      obj.ops = message.ops.map((e) => DeltaOp.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Delta>, I>>(base?: I): Delta {
    return Delta.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Delta>, I>>(object: I): Delta {
    const message = createBaseDelta();
    message.ackFor = object.ackFor ?? "";
    message.ops = object.ops?.map((e) => DeltaOp.fromPartial(e)) || [];
    return message;
  },
};

function createBaseError(): Error {
  return { code: "", message: "" };
}

export const Error: MessageFns<Error> = {
  encode(message: Error, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.code !== "") {
      writer.uint32(10).string(message.code);
    }
    if (message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Error {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseError();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.code = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Error {
    return {
      code: isSet(object.code) ? globalThis.String(object.code) : "",
      message: isSet(object.message) ? globalThis.String(object.message) : "",
    };
  },

  toJSON(message: Error): unknown {
    const obj: any = {};
    if (message.code !== "") {
      obj.code = message.code;
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Error>, I>>(base?: I): Error {
    return Error.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Error>, I>>(object: I): Error {
    const message = createBaseError();
    message.code = object.code ?? "";
    message.message = object.message ?? "";
    return message;
  },
};

function createBaseClientFrame(): ClientFrame {
  return { push: undefined };
}

export const ClientFrame: MessageFns<ClientFrame> = {
  encode(message: ClientFrame, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.push !== undefined) {
      Push.encode(message.push, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ClientFrame {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseClientFrame();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.push = Push.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ClientFrame {
    return { push: isSet(object.push) ? Push.fromJSON(object.push) : undefined };
  },

  toJSON(message: ClientFrame): unknown {
    const obj: any = {};
    if (message.push !== undefined) {
      obj.push = Push.toJSON(message.push);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ClientFrame>, I>>(base?: I): ClientFrame {
    return ClientFrame.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ClientFrame>, I>>(object: I): ClientFrame {
    const message = createBaseClientFrame();
    message.push = (object.push !== undefined && object.push !== null) ? Push.fromPartial(object.push) : undefined;
    return message;
  },
};

function createBaseServerFrame(): ServerFrame {
  return { ready: undefined, delta: undefined, error: undefined };
}

export const ServerFrame: MessageFns<ServerFrame> = {
  encode(message: ServerFrame, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ready !== undefined) {
      Ready.encode(message.ready, writer.uint32(10).fork()).join();
    }
    if (message.delta !== undefined) {
      Delta.encode(message.delta, writer.uint32(18).fork()).join();
    }
    if (message.error !== undefined) {
      Error.encode(message.error, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ServerFrame {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseServerFrame();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.ready = Ready.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.delta = Delta.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.error = Error.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ServerFrame {
    return {
      ready: isSet(object.ready) ? Ready.fromJSON(object.ready) : undefined,
      delta: isSet(object.delta) ? Delta.fromJSON(object.delta) : undefined,
      error: isSet(object.error) ? Error.fromJSON(object.error) : undefined,
    };
  },

  toJSON(message: ServerFrame): unknown {
    const obj: any = {};
    if (message.ready !== undefined) {
      obj.ready = Ready.toJSON(message.ready);
    }
    if (message.delta !== undefined) {
      obj.delta = Delta.toJSON(message.delta);
    }
    if (message.error !== undefined) {
      obj.error = Error.toJSON(message.error);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ServerFrame>, I>>(base?: I): ServerFrame {
    return ServerFrame.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ServerFrame>, I>>(object: I): ServerFrame {
    const message = createBaseServerFrame();
    message.ready = (object.ready !== undefined && object.ready !== null) ? Ready.fromPartial(object.ready) : undefined;
    message.delta = (object.delta !== undefined && object.delta !== null) ? Delta.fromPartial(object.delta) : undefined;
    message.error = (object.error !== undefined && object.error !== null) ? Error.fromPartial(object.error) : undefined;
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
