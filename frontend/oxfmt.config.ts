import { defineConfig } from "oxfmt";

export default defineConfig({
  ignorePatterns: [],
  printWidth: 80,
  singleQuote: false,
  semi: true,
  sortImports: true,
  tabWidth: 2,
  useTabs: false,
});
